[project]
name = "distributed-kv-cache-tests"
version = "0.1.0"
description = "Tests for distributed KV cache system"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "httpx>=0.28.1",
    "pytest>=8.0.0",
    "pytest-asyncio>=0.23.0",
]
